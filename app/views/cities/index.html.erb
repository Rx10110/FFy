<div class="panel panel-primary">

    <div class="panel-heading">CITIES</div>

    <table class="table table-striped grey">
        <% @cities.each do |city| %>
        <tr>
            <%= link_to city.city, city %>
        </tr>
        <% end %>
    </div>
    
</div>
